---
phase: 06-data-loading
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/server/mod.rs
  - src/server/routes/mod.rs
  - src/server/routes/jobs.rs
  - src/server/state.rs
autonomous: true
requirements:
  - LOAD-07
  - RESULT-03
  - RESULT-04

must_haves:
  truths:
    - API has POST /api/v1/load endpoint accepting {start_date, days, sync?}
    - API validates request parameters (date format, days range)
    - Sync mode returns 200 with complete job result
    - Async mode returns 202 with job-id and poll URL
    - API has GET /api/v1/jobs/{job-id} endpoint returning full job details
    - Job status response includes payload, result/error, timestamps
    - Failed jobs include error code and message in error field
  artifacts:
    - path: src/server/routes/mod.rs
      provides: Route module exports
      exports: [load_routes, job_routes]
    - path: src/server/routes/jobs.rs
      provides: Job API endpoints
      exports: [load_handler, get_job_handler]
    - path: src/server/state.rs
      provides: Application state with JobExecutor
      exports: [AppState]
  key_links:
    - from: POST /api/v1/load
      to: JobExecutor::execute_sync
      via: handler function when sync=true
    - from: POST /api/v1/load
      to: JobExecutor::execute_async
      via: handler function when sync=false
    - from: GET /api/v1/jobs/{job-id}
      to: JobRepository::get_job
      via: handler function
    - from: AppState
      to: JobExecutor
      via: Arc<JobExecutor> field
---

<objective>
Add HTTP API endpoints for data loading and job status retrieval.

Purpose: Enable programmatic access to data loading via REST API with both synchronous and asynchronous execution modes, plus job status polling.

Output: Extended server with POST /api/v1/load and GET /api/v1/jobs/{job-id} endpoints.
</objective>

<execution_context>
@/home/bruce/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bruce/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bruce/Documents/projects/astro-clock/.planning/PROJECT.md
@/home/bruce/Documents/projects/astro-clock/.planning/ROADMAP.md
@/home/bruce/Documents/projects/astro-clock/.planning/STATE.md
@/home/bruce/Documents/projects/astro-clock/.planning/phases/06-data-loading/06-RESEARCH.md
@/home/bruce/Documents/projects/astro-clock/.planning/phases/06-data-loading/06-01-SUMMARY.md
@/home/bruce/Documents/projects/astro-clock/src/server/mod.rs
@/home/bruce/Documents/projects/astro-clock/src/jobs/executor.rs
@/home/bruce/Documents/projects/astro-clock/src/jobs/types.rs
</context>

<tasks>

<task type="auto">
  <name>Create server routes module structure</name>
  <files>src/server/routes/mod.rs, src/server/routes/jobs.rs</files>
  <action>
Create server routes module for organized API endpoints:

**src/server/routes/mod.rs:**
- Module documentation
- pub mod jobs declaration
- Re-export job-related handlers

**src/server/routes/jobs.rs:**
- Request/response structs with serde derive:
  - LoadRequest { start_date: String, days: i64, sync: Option<bool> }
  - LoadResponse (sync mode): { job_id, status, result }
  - LoadResponse (async mode): { job_id, status, poll_url }
  - JobResponse: { job_id, job_type, status, payload, result, error, created_at, updated_at, started_at, completed_at }

- load_handler function:
  - Extract State<AppState> and Json<LoadRequest>
  - Validate days is 1-365, return 400 if invalid
  - Validate date format, return 400 if invalid
  - Build payload JSON
  - If sync=true or sync missing:
    - Call executor.execute_sync(JobType::Load, payload).await
    - Return 200 with job details and result
  - If sync=false:
    - Call executor.execute_async(JobType::Load, payload).await
    - Return 202 with job-id and poll URL

- get_job_handler function:
  - Extract Path<Uuid> for job-id
  - Call repository.get_job(job_id).await
  - Return 404 if job not found
  - Return 200 with full job details including payload, result/error, all timestamps
  - Error field must include code and message if job failed

Reference 06-RESEARCH.md API integration examples for patterns.
  </action>
  <verify>
    <automated>cargo check --features db 2>&1 | grep -E "error" | head -5 || echo "No errors"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>
    - Routes module structure created
    - Request/response structs defined with serde
    - Handler function signatures correct
    - No compilation errors
  </done>
</task>

<task type="auto">
  <name>Create AppState for server</name>
  <files>src/server/state.rs</files>
  <action>
Create src/server/state.rs with application state shared across handlers:

```rust
use std::sync::Arc;
use crate::jobs::executor::JobExecutor;

#[derive(Clone)]
pub struct AppState {
    pub executor: Arc<JobExecutor>,
}

impl AppState {
    pub fn new(executor: JobExecutor) -> Self {
        Self {
            executor: Arc::new(executor),
        }
    }
}
```

This allows handlers to access JobExecutor via State<AppState> extractor.

Ensure this integrates with existing server patterns in src/server/mod.rs.
  </action>
  <verify>
    <automated>cargo check --features db 2>&1 | grep -E "error" | head -5 || echo "No errors"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>
    - AppState struct created with Arc<JobExecutor>
    - State can be extracted in axum handlers
    - Clone implementation correct for sharing
  </done>
</task>

<task type="auto">
  <name>Integrate routes into server</name>
  <files>src/server/mod.rs</files>
  <action>
Update src/server/mod.rs to integrate the new API routes:

1. Add imports for new routes module and AppState
2. Modify Server struct to hold database pool or connection info
3. Update run() method to:
   - Create database pool
   - Create JobRepository and LoadJobHandler
   - Create JobExecutor with LoadJobHandler registered
   - Create AppState with executor
   - Build router with new routes:
     - POST /api/v1/load -> load_handler
     - GET /api/v1/jobs/:id -> get_job_handler
   - Apply State layer to routes

**Route structure:**
```rust
let app = axum::Router::new()
    .route("/health", get(health_handler))
    .route("/chart", get(chart_handler))
    .route("/api/v1/load", post(load_handler))
    .route("/api/v1/jobs/:id", get(get_job_handler))
    .with_state(app_state);
```

Ensure existing /health and /chart routes continue to work.

Handle database initialization errors gracefully with appropriate logging.
  </action>
  <verify>
    <automated>cargo check --features db 2>&1 | grep -E "error" | head -5 || echo "No errors"</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>
    - Server integrates routes module
    - Database pool created and passed to executor
    - JobExecutor created with LoadJobHandler registered
    - Routes accessible at correct paths
    - Existing routes still functional
  </done>
</task>

</tasks>

<verification>
**Compilation:** cargo check --features db passes
**Test (if db available):**
- POST /api/v1/load {"start_date":"2024-01-01","days":7} → returns 202 with job-id
- POST /api/v1/load {"start_date":"2024-01-01","days":7,"sync":true} → returns 200 with result
- GET /api/v1/jobs/{job-id} → returns full job details
**Validation:**
- Invalid date format → 400 Bad Request
- Days out of range → 400 Bad Request
- Non-existent job-id → 404 Not Found
</verification>

<success_criteria>
1. POST /api/v1/load accepts {start_date, days, sync?} JSON
2. Sync mode (sync=true) returns 200 with complete job and result
3. Async mode (sync=false or omitted) returns 202 with job-id and poll URL
4. GET /api/v1/jobs/{job-id} returns full job details (payload, result/error, timestamps)
5. Failed jobs include error with code/message in response
6. Input validation returns 400 for invalid dates or days out of range
7. Non-existent job returns 404
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-loading/06-03-SUMMARY.md`
</output>
