---
phase: 04-performance
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/chunk_generator.rs
autonomous: true
gap_closure: true
requirements:
  - PERF-03

must_haves:
  truths:
    - "is_major_aspect() is called in calculate_aspects() to filter aspects"
    - "Only aspects within 8° orb of major angles (0°, 60°, 90°, 120°, 180°) are stored"
    - "Storage reduction of ~80% is achieved compared to storing all aspects"
    - "No dead code warnings for unused MAJOR_ASPECT_ANGLES constant"
    - "No dead code warnings for unused is_major_aspect() function"
  artifacts:
    - path: "src/database/chunk_generator.rs"
      provides: "Integrated aspect filtering in calculate_aspects()"
      contains: "is_major_aspect(diff) check before pushing to aspects vector"
  key_links:
    - from: "calculate_aspects()"
      to: "is_major_aspect()"
      via: "Function call at aspect filtering point"
      pattern: "if !is_major_aspect(diff) { continue; }"
---

<objective>
Integrate the existing `is_major_aspect()` function into `calculate_aspects()` to filter minor aspects before storage, achieving the planned ~80% storage reduction.

Purpose: The aspect filtering infrastructure exists but is not connected. This gap closure connects the filtering logic to achieve the storage optimization goal.
Output: Modified chunk_generator.rs with integrated aspect filtering and no dead code warnings.
</objective>

<execution_context>
@/home/bruce/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bruce/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-performance/04-VERIFICATION.md
@src/database/chunk_generator.rs

## Gap Context

From VERIFICATION.md Gap 1:
- **Issue:** `is_major_aspect()` function defined but never called in `calculate_aspects()`
- **Location:** src/database/chunk_generator.rs lines 10-23 (unused code), lines 167-231 (calculate_aspects without filtering)
- **Impact:** All aspects are being calculated and stored, not just major aspects
- **Required Fix:** Call `is_major_aspect(diff)` in the aspect calculation loop to skip minor aspects

Current state of calculate_aspects():
- Lines 201-227: Iterates through ASPECT_TYPE_IDS and stores all matching aspects
- Missing: Filter to only store if the angular separation is a major aspect within orb

The `is_major_aspect()` function (lines 16-23) correctly checks if an angle is within 8° of major aspect angles (0°, 60°, 90°, 120°, 180°).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate aspect filtering into calculate_aspects()</name>
  <files>src/database/chunk_generator.rs</files>
  <action>
    Modify the `calculate_aspects()` method to call `is_major_aspect()` before storing aspects.
    
    Current logic (lines 196-227):
    1. Calculate diff between body longitudes
    2. Loop through ASPECT_TYPE_IDS
    3. Check if orb <= ASPECT_ORB
    4. Push aspect to vector
    
    New logic:
    1. Calculate diff between body longitudes
    2. **NEW:** Check if `is_major_aspect(diff)` returns true - if not, continue to next pair
    3. Loop through ASPECT_TYPE_IDS
    4. Check if orb <= ASPECT_ORB
    5. Push aspect to vector
    
    The check should happen after calculating `diff` (line 196-197) but before the ASPECT_TYPE_IDS loop (line 201).
    
    Also remove the unused `MAJOR_ASPECT_ANGLES` constant (lines 9-10) since `is_major_aspect()` uses its own inline array.
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -E "(warning:.*MAJOR_ASPECT_ANGLES|warning:.*is_major_aspect)" | wc -l | grep -q "^0$"</automated>
    <manual>Verify no dead code warnings for MAJOR_ASPECT_ANGLES or is_major_aspect</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>is_major_aspect() is called in calculate_aspects() to filter aspects, and no dead code warnings exist</done>
</task>

<task type="auto">
  <name>Task 2: Verify aspect filtering logic and add test</name>
  <files>src/database/chunk_generator.rs</files>
  <action>
    Add a unit test to verify that aspect filtering works correctly.
    
    The test should:
    1. Create mock planet positions with known angular separations
    2. Call calculate_aspects() 
    3. Verify that only major aspects (within 8° of 0°, 60°, 90°, 120°, 180°) are returned
    4. Verify that minor aspects (e.g., 30°, 150°) are filtered out
    
    Add the test to the existing #[cfg(test)] module at the bottom of the file.
  </action>
  <verify>
    <automated>cargo test chunk_generator::tests::test_aspect_filtering -- --nocapture</automated>
    <manual>Verify test passes and confirms only major aspects are stored</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Unit test confirms aspect filtering works correctly, only major aspects stored</done>
</task>

</tasks>

<verification>
1. Build completes with no dead code warnings for MAJOR_ASPECT_ANGLES or is_major_aspect
2. Unit test passes confirming aspect filtering works
3. Code review confirms is_major_aspect() is called before storing aspects
4. Storage reduction target (~80%) is achievable with filtering enabled
</verification>

<success_criteria>
- [ ] `is_major_aspect()` is called in `calculate_aspects()` to filter aspects
- [ ] No compiler warnings for unused MAJOR_ASPECT_ANGLES constant
- [ ] No compiler warnings for unused is_major_aspect() function
- [ ] Unit test verifies only major aspects are stored
- [ ] All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-performance/04-06-SUMMARY.md`
</output>
