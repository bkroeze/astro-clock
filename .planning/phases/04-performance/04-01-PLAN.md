---
phase: 04-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/006_create_continuous_aggregates.sql
  - src/database/multi_resolution.rs
  - src/database/mod.rs
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - TimescaleDB continuous aggregates exist for 5-minute and 60-minute resolutions
    - Planet positions are downsampled by category (Moon 1min, inner 5min, outer 60min)
    - Continuous aggregates have automatic refresh policies configured
    - Query system can load data from appropriate resolution based on planet category
  artifacts:
    - path: migrations/006_create_continuous_aggregates.sql
      provides: Database migration for continuous aggregates
      contains: CREATE MATERIALIZED VIEW ... WITH (timescaledb.continuous)
    - path: src/database/multi_resolution.rs
      provides: Multi-resolution data loading and management
      exports:
        - MultiResolutionManager
        - load_positions_for_body
        - get_resolution_for_body
  key_links:
    - from: src/database/multi_resolution.rs
      to: migrations/006_create_continuous_aggregates.sql
      via: SQL queries against continuous aggregate views
      pattern: SELECT.*FROM planet_positions_(5min|60min)
---

<objective>
Implement multi-resolution storage using TimescaleDB continuous aggregates to reduce database storage by storing outer planets at lower resolution (60-minute) while maintaining 1-minute resolution for Moon and 5-minute for inner planets.

Purpose: Reduce annual storage from ~2GB/year to <50GB/year target by downsampling slowly-moving outer planets (PERF-01, PERF-03)
Output: Database migration for continuous aggregates, Rust module for multi-resolution data loading
</objective>

<execution_context>
@/home/bruce/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bruce/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-performance/04-CONTEXT.md
@.planning/phases/04-performance/04-RESEARCH.md

Existing code patterns:
- src/database/chunk.rs - Compact struct patterns for memory efficiency
- src/database/chunk_manager.rs - Database loading with sqlx query patterns
- src/database/schema.rs - PlanetPosition struct with body_id field
- migrations/001_create_hypertables.sql - TimescaleDB hypertable creation pattern

User decisions (LOCKED):
- Moon at 1-minute, inner planets at 5-minute, outer planets at 60-minute resolution
- Use TimescaleDB continuous aggregates with `last` aggregation
- No compression (deferred to avoid overhead)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create continuous aggregates migration</name>
  <files>migrations/006_create_continuous_aggregates.sql</files>
  <action>
Create a new migration file that defines TimescaleDB continuous aggregates for multi-resolution storage.

SQL to include:
1. Continuous aggregate for 5-minute resolution (inner planets: Sun=0, Mercury=2, Venus=3, Mars=4):
   - Use time_bucket('5 minutes', time) for bucketing
   - Use last(column, time) for all value columns
   - Filter WHERE body_id IN (0, 2, 3, 4)
   - Group by bucket, body_id

2. Continuous aggregate for 60-minute resolution (outer planets: Jupiter=5, Saturn=6, Uranus=7, Neptune=8, Pluto=9):
   - Use time_bucket('60 minutes', time) for bucketing
   - Use last(column, time) for all value columns
   - Filter WHERE body_id IN (5, 6, 7, 8, 9)
   - Group by bucket, body_id

3. Refresh policies for both aggregates:
   - 5min aggregate: start_offset='1 day', end_offset='1 hour', schedule_interval='1 hour'
   - 60min aggregate: start_offset='7 days', end_offset='1 hour', schedule_interval='1 hour'

Columns to include in both views:
- bucket (timestamp)
- body_id (SMALLINT)
- longitude (DECIMAL(8,4))
- latitude (DECIMAL(8,4))
- distance (DECIMAL(8,4))
- speed_lon (DECIMAL(8,4))
- retrograde (BOOLEAN)
- zodiac_sign (SMALLINT)

Use IF NOT EXISTS to make migration idempotent.
  </action>
  <verify>
    <automated>test -f migrations/006_create_continuous_aggregates.sql && grep -q "CREATE MATERIALIZED VIEW.*planet_positions_5min" migrations/006_create_continuous_aggregates.sql && grep -q "CREATE MATERIALIZED VIEW.*planet_positions_60min" migrations/006_create_continuous_aggregates.sql && grep -q "add_continuous_aggregate_policy" migrations/006_create_continuous_aggregates.sql</automated>
  </verify>
  <done>Migration file exists with both continuous aggregate definitions and refresh policies</done>
</task>

<task type="auto">
  <name>Task 2: Implement multi-resolution manager module</name>
  <files>src/database/multi_resolution.rs</files>
  <action>
Create src/database/multi_resolution.rs with the following components:

1. Body category enum:
   ```rust
   #[derive(Debug, Clone, Copy, PartialEq, Eq)]
   pub enum BodyCategory {
       Moon,        // 1-minute resolution
       Inner,       // 5-minute resolution (Sun, Mercury, Venus, Mars)
       Outer,       // 60-minute resolution (Jupiter, Saturn, Uranus, Neptune, Pluto)
   }
   ```

2. Resolution enum:
   ```rust
   #[derive(Debug, Clone, Copy, PartialEq, Eq)]
   pub enum Resolution {
       OneMinute,   // For Moon
       FiveMinute,  // For inner planets
       SixtyMinute, // For outer planets
   }
   ```

3. Helper functions:
   - `get_body_category(body_id: i16) -> BodyCategory` - Map body_id to category
   - `get_resolution_for_body(body_id: i16) -> Resolution` - Get appropriate resolution
   - `get_table_for_resolution(res: Resolution) -> &'static str` - Map to table name

4. MultiResolutionManager struct:
   ```rust
   pub struct MultiResolutionManager {
       pool: DatabasePool,
   }
   ```

5. Implementation methods:
   - `new(pool: DatabasePool) -> Self`
   - `async fn load_positions_for_body(&self, body_id: i16, start: DateTime<Utc>, end: DateTime<Utc>) -> Result<Vec<schema::PlanetPosition>, sqlx::Error>` - Load from appropriate resolution table based on body_id
   - `async fn load_positions_multi_resolution(&self, body_ids: &[i16], start: DateTime<Utc>, end: DateTime<Utc>) -> Result<Vec<schema::PlanetPosition>, sqlx::Error>` - Load multiple bodies, querying appropriate table for each

For the load_positions_for_body method:
- Determine resolution based on body_id
- Query the appropriate table (planet_positions for Moon, planet_positions_5min for inner, planet_positions_60min for outer)
- Convert f64 results to Decimal using Decimal::from_f64_retain() (follow chunk_manager.rs pattern)
- Return Vec<schema::PlanetPosition>

Body ID mapping (from schema):
- 0 = Sun (Inner)
- 1 = Moon (Moon)
- 2 = Mercury (Inner)
- 3 = Venus (Inner)
- 4 = Mars (Inner)
- 5 = Jupiter (Outer)
- 6 = Saturn (Outer)
- 7 = Uranus (Outer)
- 8 = Neptune (Outer)
- 9 = Pluto (Outer)
  </action>
  <verify>
    <automated>cargo check --features db 2>&1 | grep -q "error" && echo "CHECK FAILED" || echo "CHECK PASSED"</automated>
  </verify>
  <done>Module compiles with all types and methods, follows existing code patterns</done>
</task>

<task type="auto">
  <name>Task 3: Export multi-resolution module</name>
  <files>src/database/mod.rs</files>
  <action>
Update src/database/mod.rs to export the multi_resolution module:

1. Add module declaration:
   ```rust
   pub mod multi_resolution;
   ```

2. Re-export key types for convenience:
   ```rust
   pub use multi_resolution::{MultiResolutionManager, BodyCategory, Resolution, get_body_category, get_resolution_for_body};
   ```

Place the new module declaration after the existing chunk-related modules to maintain logical grouping.
  </action>
  <verify>
    <automated>cargo check --features db 2>&1 | grep -q "error" && echo "CHECK FAILED" || echo "CHECK PASSED"</automated>
  </verify>
  <done>Module is properly exported and accessible from other parts of the codebase</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Migration file contains valid SQL for continuous aggregates
2. MultiResolutionManager can load data from appropriate resolution tables
3. Body categorization correctly maps IDs to resolutions
4. Module exports are accessible
5. Code compiles with --features db
</verification>

<success_criteria>
- TimescaleDB continuous aggregates exist for 5-minute and 60-minute resolutions
- MultiResolutionManager can query appropriate tables based on body category
- Body ID to resolution mapping is correct (Moon=1min, inner=5min, outer=60min)
- Module is properly integrated into database module hierarchy
</success_criteria>

<output>
After completion, create `.planning/phases/04-performance/04-01-SUMMARY.md`
</output>
