---
phase: 01-database-schema
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - migrations/verify_schema.sql
  - src/database/schema.rs
autonomous: false
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05
  - DB-06
must_haves:
  truths:
    - All migrations can be applied successfully to a database
    - Schema verification script confirms all tables exist
    - Rust schema module reflects the database structure
    - TimescaleDB extension is confirmed active
  artifacts:
    - path: migrations/verify_schema.sql
      provides: Schema verification queries
      min_lines: 30
    - path: src/database/schema.rs
      provides: Rust types for database tables
      min_lines: 100
  key_links:
    - from: src/database/schema.rs
      to: database tables
      via: sqlx FromRow derives
      pattern: #\[derive.*FromRow
---

<objective>
Create schema verification script and update Rust schema module to reflect the new database structure.

Purpose: Enable verification that migrations applied correctly and provide Rust types for database operations.
Output: SQL verification script and updated Rust schema module with table structs.
</objective>

<execution_context>
@/home/bruce/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bruce/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-database-schema/01-RESEARCH.md
@src/database/schema.rs
@migrations/001_create_hypertables.sql
@migrations/002_create_indexes.sql
@migrations/003_create_locations.sql
@migrations/004_create_aspect_summaries.sql
</context>

<tasks>

<task type="auto">
  <name>Create schema verification SQL script</name>
  <files>migrations/verify_schema.sql</files>
  <action>
Create SQL script to verify schema was created correctly:

1. Check TimescaleDB extension is installed:
   - SELECT * FROM pg_extension WHERE extname = 'timescaledb';

2. Verify all tables exist:
   - planet_positions
   - aspects
   - lunar_conditions
   - house_cusps
   - locations
   - aspect_summaries

3. Verify hypertables were created:
   - SELECT hypertable_name FROM timescaledb_information.hypertables;
   - Check chunk_time_interval is 1 day

4. Verify indexes exist:
   - Check composite indexes on planet_positions
   - Check partial index for Moon
   - Check indexes on aspects, lunar_conditions, house_cusps
   - Check aspect_summaries index

5. Verify foreign keys:
   - house_cusps.location_id -> locations.id

6. Count verification (sanity checks):
   - All tables should have 0 rows initially
   - Confirm tables are ready for data loading

Include comments explaining each check and expected results.
  </action>
  <verify>
    <automated>grep -E "SELECT.*FROM|hypertable|planet_positions|aspect_summaries" migrations/verify_schema.sql | head -20</automated>
    <manual>Verify script checks all tables, hypertables, indexes, and foreign keys</manual>
  </verify>
  <done>Schema verification script created with queries for all schema objects</done>
</task>

<task type="auto">
  <name>Update Rust schema module with new table structs</name>
  <files>src/database/schema.rs</files>
  <action>
Update src/database/schema.rs to add Rust structs for new tables:

Keep existing ChartRecord and PlanetPositionRecord for backward compatibility, but add new structs:

1. PlanetPosition struct (for planet_positions table):
   - time: chrono::DateTime<chrono::Utc>
   - body_id: i16
   - longitude: rust_decimal::Decimal
   - latitude: rust_decimal::Decimal
   - distance: rust_decimal::Decimal
   - speed_lon: rust_decimal::Decimal
   - retrograde: bool
   - zodiac_sign: i16
   - Derive FromRow, Debug, Clone

2. Aspect struct (for aspects table):
   - time: chrono::DateTime<chrono::Utc>
   - body1_id: i16
   - body2_id: i16
   - aspect_type: i16
   - orb: rust_decimal::Decimal
   - applying: bool
   - Derive FromRow, Debug, Clone

3. LunarCondition struct (for lunar_conditions table):
   - time: chrono::DateTime<chrono::Utc>
   - moon_phase: i16
   - moon_sign: i16
   - moon_phase_angle: rust_decimal::Decimal
   - moon_illumination: rust_decimal::Decimal
   - is_void_of_course: bool
   - voc_start: Option<chrono::DateTime<chrono::Utc>>
   - voc_end: Option<chrono::DateTime<chrono::Utc>>
   - Derive FromRow, Debug, Clone

4. Location struct (for locations table):
   - id: i32
   - name: Option<String>
   - latitude: rust_decimal::Decimal
   - longitude: rust_decimal::Decimal
   - timezone: Option<String>
   - Derive FromRow, Debug, Clone

5. HouseCusp struct (for house_cusps table):
   - time: chrono::DateTime<chrono::Utc>
   - location_id: i32
   - house_system: i16
   - cusp_1 through cusp_12: rust_decimal::Decimal
   - ascendant: rust_decimal::Decimal
   - mc: rust_decimal::Decimal
   - Derive FromRow, Debug, Clone

6. AspectSummary struct (for aspect_summaries table):
   - time: chrono::DateTime<chrono::Utc>
   - body_id: i16
   - conjunctions: i16
   - sextiles: i16
   - squares: i16
   - trines: i16
   - oppositions: i16
   - total_favorable: i16
   - total_challenging: i16
   - Derive FromRow, Debug, Clone

Use appropriate types matching the SQL schema (i16 for SMALLINT, Decimal for DECIMAL). Add doc comments explaining each struct and its purpose.
  </action>
  <verify>
    <automated>grep -c "struct.*FromRow\|#\[derive.*FromRow" src/database/schema.rs</automated>
    <manual>Verify all 6 new structs exist with proper fields and FromRow derives</manual>
  </verify>
  <done>Rust schema module updated with structs for all new tables</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify schema with live database</name>
  <files>N/A</files>
  <action>
Human verification step to confirm migrations work against a real database:

Prerequisites:
- PostgreSQL with TimescaleDB extension installed
- PG_URL environment variable set

Steps to verify:
1. Run: just migrate
2. Run: psql $PG_URL -f migrations/verify_schema.sql
3. Verify all checks pass (all tables exist, hypertables created, indexes present)

Expected results:
- 6 tables exist
- 4 hypertables (planet_positions, aspects, lunar_conditions, house_cusps)
- 8+ indexes
- 1 foreign key (house_cusps -> locations)
- All tables have 0 rows (ready for data)
  </action>
  <verify>
    <automated>MISSING — Requires live database connection</automated>
    <manual>Run just migrate and verify_schema.sql against database to confirm all schema objects created correctly</manual>
  </verify>
  <done>Schema verified working against live PostgreSQL/TimescaleDB instance</done>
</task>

</tasks>

<verification>
Schema verification complete:
- verify_schema.sql script created ✓
- Rust schema module updated with all table structs ✓
- Human verification confirms migrations work ✓

All requirements addressed:
- DB-01: aspect_summaries table ✓
- DB-02: composite indexes ✓
- DB-03: house_cusps with locations table ✓
- DB-04: planet_positions hypertable ✓
- DB-05: aspects hypertable ✓
- DB-06: lunar_conditions hypertable ✓
</verification>

<success_criteria>
- Schema verification script can be run against database
- All 6 tables exist and are queryable
- 4 hypertables have 1-day chunk intervals
- All indexes are created
- Rust structs match database schema
- Foreign key relationship established
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-schema/01-03-SUMMARY.md`
</output>
