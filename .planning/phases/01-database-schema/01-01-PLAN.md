---
phase: 01-database-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/001_create_hypertables.sql
  - migrations/002_create_indexes.sql
  - migrations/003_create_locations.sql
  - migrations/004_create_aspect_summaries.sql
autonomous: true
requirements:
  - DB-04
  - DB-05
  - DB-06
  - DB-03
must_haves:
  truths:
    - All hypertables created with proper chunk intervals (1 day)
    - Locations table normalizes lat/lon data
    - Aspect summaries table exists for pre-aggregated data
    - Migration scripts are valid SQL and follow TimescaleDB conventions
  artifacts:
    - path: migrations/001_create_hypertables.sql
      provides: Planet positions, aspects, lunar conditions, house cusps hypertables
      min_lines: 80
    - path: migrations/002_create_indexes.sql
      provides: Composite and partial indexes for query optimization
      min_lines: 50
    - path: migrations/003_create_locations.sql
      provides: Normalized locations table and house_cusps refactor
      min_lines: 40
    - path: migrations/004_create_aspect_summaries.sql
      provides: Pre-aggregated aspect counts table
      min_lines: 30
  key_links:
    - from: house_cusps
      to: locations
      via: location_id foreign key
      pattern: REFERENCES locations\(id\)
    - from: aspect_summaries
      to: planet_positions
      via: time + body_id
      pattern: time.*body_id
---

<objective>
Create TimescaleDB hypertables and supporting tables for astrological time-series data.

Purpose: Establish the foundation for efficient storage and querying of planet positions, aspects, lunar conditions, and house cusps with 1-minute resolution.
Output: Four migration files creating all necessary tables with proper TimescaleDB configuration.
</objective>

<execution_context>
@/home/bruce/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bruce/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-database-schema/01-CONTEXT.md
@.planning/phases/01-database-schema/01-RESEARCH.md
@implementation_plan.md
@src/database/schema.rs
@src/database/pool.rs
</context>

<tasks>

<task type="auto">
  <name>Create core hypertables migration (001)</name>
  <files>migrations/001_create_hypertables.sql</files>
  <action>
Create migration file with TimescaleDB hypertables for time-series astrological data:

1. Create `planet_positions` hypertable:
   - time (TIMESTAMPTZ NOT NULL) - partition key
   - body_id (SMALLINT NOT NULL) - 0-9 for Sun, Moon, planets
   - longitude (DECIMAL(8,4)) - 0-360 degrees
   - latitude (DECIMAL(8,4))
   - distance (DECIMAL(10,6)) - AU
   - speed_lon (DECIMAL(9,5)) - daily motion
   - retrograde (BOOLEAN)
   - zodiac_sign (SMALLINT) - 0-11
   - Convert to hypertable with 1-day chunks

2. Create `aspects` hypertable:
   - time (TIMESTAMPTZ NOT NULL)
   - body1_id (SMALLINT NOT NULL)
   - body2_id (SMALLINT NOT NULL)
   - aspect_type (SMALLINT) - 0=conjunction, 1=sextile, 2=square, 3=trine, 4=opposition
   - orb (DECIMAL(5,2)) - degrees from exact
   - applying (BOOLEAN) - aspect getting tighter?
   - Convert to hypertable with 1-day chunks

3. Create `lunar_conditions` hypertable:
   - time (TIMESTAMPTZ NOT NULL)
   - moon_phase (SMALLINT) - 0-7 (new, crescent, quarter, etc.)
   - moon_sign (SMALLINT) - 0-11
   - moon_phase_angle (DECIMAL(6,2)) - 0-360 degrees
   - moon_illumination (DECIMAL(5,4)) - 0.0-1.0
   - is_void_of_course (BOOLEAN)
   - voc_start (TIMESTAMPTZ) - nullable
   - voc_end (TIMESTAMPTZ) - nullable
   - Convert to hypertable with 1-day chunks

4. Create `house_cusps` hypertable (preliminary - location_id added in migration 003):
   - time (TIMESTAMPTZ NOT NULL)
   - location_id (INTEGER) - placeholder, FK added later
   - house_system (SMALLINT) - 0-10 for supported systems
   - cusp_1 through cusp_12 (DECIMAL(8,4))
   - ascendant (DECIMAL(8,4))
   - mc (DECIMAL(8,4)) - midheaven
   - Convert to hypertable with 1-day chunks

Use DECIMAL types for precise astrological calculations. Use SMALLINT for enums/IDs to save space. Include appropriate DEFAULT values and CHECK constraints where applicable.
  </action>
  <verify>
    <automated>cat migrations/001_create_hypertables.sql | head -100</automated>
    <manual>Verify file contains CREATE TABLE statements for all 4 hypertables and SELECT create_hypertable() calls with chunk_time_interval => INTERVAL '1 day'</manual>
  </verify>
  <done>Migration file exists with valid SQL for all 4 hypertables with proper TimescaleDB configuration</done>
</task>

<task type="auto">
  <name>Create indexes migration (002)</name>
  <files>migrations/002_create_indexes.sql</files>
  <action>
Create migration file with composite and partial indexes for common query patterns:

1. Planet positions indexes:
   - Composite: (time, body_id) for time-range queries by body
   - Partial: (time) WHERE body_id = 1 (Moon) - frequently queried
   - Composite: (body_id, zodiac_sign, time) WHERE body_id = 1 - for moon sign queries

2. Aspects indexes:
   - Composite: (time, body1_id, body2_id) for time-range aspect queries
   - Composite: (time, body1_id, aspect_type, orb) for filtered aspect queries

3. Lunar conditions indexes:
   - Partial: (time, is_void_of_course) WHERE is_void_of_course = true - for VoC checks
   - Covering: (time, is_void_of_course, moon_sign) INCLUDE (moon_phase_angle, moon_illumination)

4. House cusps indexes:
   - Composite: (location_id, time) for location-based queries

All indexes should use appropriate naming convention: idx_{table}_{purpose}
  </action>
  <verify>
    <automated>grep -c "CREATE INDEX" migrations/002_create_indexes.sql</automated>
    <manual>Verify at least 8 indexes created covering all query patterns from RESEARCH.md</manual>
  </verify>
  <done>Migration file exists with indexes for all common query patterns including partial indexes for Moon and VoC</done>
</task>

<task type="auto">
  <name>Create locations table migration (003)</name>
  <files>migrations/003_create_locations.sql</files>
  <action>
Create migration file for normalized locations table and house_cusps refactoring:

1. Create `locations` table:
   - id (SERIAL PRIMARY KEY)
   - name (VARCHAR(100)) - optional location name
   - latitude (DECIMAL(8,4)) NOT NULL
   - longitude (DECIMAL(8,4)) NOT NULL
   - timezone (VARCHAR(50))
   - UNIQUE constraint on (latitude, longitude)

2. Refactor house_cusps table:
   - If house_cusps exists from migration 001, alter to add foreign key
   - Otherwise ensure location_id has proper REFERENCES constraint
   - Add composite primary key: (time, location_id, house_system)

3. Add foreign key constraint:
   - house_cusps.location_id REFERENCES locations(id)
   - ON DELETE CASCADE (removing location removes its house cusps)

This normalization reduces storage by ~60% for repeated locations per implementation_plan.md.
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE.*locations|REFERENCES locations|UNIQUE.*latitude.*longitude" migrations/003_create_locations.sql</automated>
    <manual>Verify locations table has proper structure and house_cusps has FK constraint</manual>
  </verify>
  <done>Migration file exists with locations table and proper foreign key relationship to house_cusps</done>
</task>

<task type="auto">
  <name>Create aspect summaries migration (004)</name>
  <files>migrations/004_create_aspect_summaries.sql</files>
  <action>
Create migration file for aspect_summaries table (addresses DB-01):

1. Create `aspect_summaries` table (NOT a hypertable - regular table with time index):
   - time (TIMESTAMPTZ NOT NULL)
   - body_id (SMALLINT NOT NULL)
   - conjunctions (SMALLINT DEFAULT 0)
   - sextiles (SMALLINT DEFAULT 0)
   - squares (SMALLINT DEFAULT 0)
   - trines (SMALLINT DEFAULT 0)
   - oppositions (SMALLINT DEFAULT 0)
   - total_favorable (SMALLINT DEFAULT 0) - trines + sextiles
   - total_challenging (SMALLINT DEFAULT 0) - squares + oppositions
   - PRIMARY KEY (time, body_id)

2. Create index for fast lookups:
   - Composite: (time, body_id) for aspect count queries

3. Add comment explaining:
   - This table is populated by Phase 3 (Query System)
   - Pre-aggregated counts eliminate correlated subqueries
   - Enables wedding query to run in ~45ms vs 2.3s

Note: This table is created empty. Population logic will be implemented in Phase 3 when query functions are built. The table structure supports the optimized query pattern from implementation_plan.md.
  </action>
  <verify>
    <automated>grep -E "CREATE TABLE.*aspect_summaries|conjunctions|total_favorable|PRIMARY KEY" migrations/004_create_aspect_summaries.sql</automated>
    <manual>Verify aspect_summaries has all aspect type columns and proper primary key</manual>
  </verify>
  <done>Migration file exists with aspect_summaries table structure supporting pre-aggregated aspect counts</done>
</task>

</tasks>

<verification>
All migration files created:
- 001_create_hypertables.sql - 4 hypertables with TimescaleDB chunking
- 002_create_indexes.sql - Composite and partial indexes
- 003_create_locations.sql - Normalized locations + house_cusps FK
- 004_create_aspect_summaries.sql - Pre-aggregated aspect counts

Requirements addressed:
- DB-04: planet_positions hypertable ✓
- DB-05: aspects hypertable ✓
- DB-06: lunar_conditions hypertable ✓
- DB-03: house_cusps with normalized locations ✓
- DB-01: aspect_summaries table ✓
- DB-02: composite indexes ✓
</verification>

<success_criteria>
- All 4 migration files exist in migrations/ directory
- Each file contains valid TimescaleDB SQL
- Hypertables use 1-day chunk intervals
- Locations table normalizes lat/lon data
- Aspect summaries table has proper structure for pre-aggregation
- Indexes cover all query patterns from research
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-schema/01-01-SUMMARY.md`
</output>
