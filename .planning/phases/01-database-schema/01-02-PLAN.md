---
phase: 01-database-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Justfile
  - Cargo.toml
  - .env.example
autonomous: true
requirements:
  - DB-02
must_haves:
  truths:
    - Justfile has migrate command to run sqlx migrations
    - Database connection can be verified via CLI
    - Migration files are discoverable by sqlx
  artifacts:
    - path: Justfile
      provides: just migrate command
      contains: migrate recipe
    - path: Cargo.toml
      provides: sqlx-cli dependency documentation
      unchanged: true
  key_links:
    - from: just migrate
      to: sqlx migrate run
      via: Justfile recipe
      pattern: sqlx migrate run
---

<objective>
Set up sqlx migration tooling and Justfile integration for database management.

Purpose: Enable developers to run migrations easily and ensure sqlx can discover migration files.
Output: Updated Justfile with migrate command, working migration discovery.
</objective>

<execution_context>
@/home/bruce/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/bruce/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-database-schema/01-CONTEXT.md
@.planning/phases/01-database-schema/01-RESEARCH.md
@Cargo.toml
@Justfile
</context>

<tasks>

<task type="auto">
  <name>Add migration commands to Justfile</name>
  <files>Justfile</files>
  <action>
Read the existing Justfile and add migration-related recipes:

1. Add `migrate` recipe that runs: sqlx migrate run --database-url "${PG_URL}"
2. Add `migrate-create` recipe for creating new migrations: sqlx migrate add
3. Add `migrate-revert` recipe: sqlx migrate revert --database-url "${PG_URL}"
4. Add `migrate-info` recipe: sqlx migrate info --database-url "${PG_URL}"
5. Add `db-setup` recipe that:
   - Checks PG_URL is set
   - Runs migrations
   - Verifies connection

Include helpful comments and ensure recipes handle the PG_URL environment variable properly. Use the existing Justfile style and conventions.
  </action>
  <verify>
    <automated>grep -E "^migrate:|^db-setup:" Justfile</automated>
    <manual>Verify Justfile contains migrate recipes and follows existing conventions</manual>
  </verify>
  <done>Justfile updated with migrate, migrate-create, migrate-revert, migrate-info, and db-setup recipes</done>
</task>

<task type="auto">
  <name>Create .env.example with database configuration</name>
  <files>.env.example</files>
  <action>
Create .env.example file documenting required environment variables:

1. PG_URL - PostgreSQL connection string
   - Example: postgresql://user:password@host:port/database
   - Note about TimescaleDB requirement

2. RUST_LOG - Optional logging level
   - Example: info, debug, error

3. SE_EPHE_PATH - Swiss Ephemeris data path
   - Example: ./data

Include comments explaining each variable and its purpose. This serves as documentation for developers setting up the project.
  </action>
  <verify>
    <automated>grep "PG_URL" .env.example</automated>
    <manual>Verify .env.example contains all documented environment variables</manual>
  </verify>
  <done>.env.example created with PG_URL and other environment variables documented</done>
</task>

<task type="auto">
  <name>Verify sqlx migration structure</name>
  <files>migrations/</files>
  <action>
Ensure migrations directory structure is compatible with sqlx:

1. Verify migrations/ directory exists (created in Plan 01)
2. Verify migration files follow sqlx naming convention: {version}_{description}.sql
   - 001_create_hypertables.sql ✓
   - 002_create_indexes.sql ✓
   - 003_create_locations.sql ✓
   - 004_create_aspect_summaries.sql ✓

3. Check that no sqlx-specific metadata conflicts exist
4. Document in a README or comment that these migrations use sqlx conventions

Note: sqlx looks for migrations in the ./migrations directory by default. Our naming convention (NNN_description.sql) is compatible.
  </action>
  <verify>
    <automated>ls migrations/*.sql | wc -l</automated>
    <manual>Verify 4 migration files exist with proper naming convention</manual>
  </verify>
  <done>Migration directory structure verified and compatible with sqlx tooling</done>
</task>

</tasks>

<verification>
Migration tooling set up:
- Justfile has migrate command ✓
- .env.example documents PG_URL ✓
- Migration files follow sqlx conventions ✓

Requirements addressed:
- DB-02: Indexes created in migration 002 ✓
- Tooling enables future migration management ✓
</verification>

<success_criteria>
- just migrate command available and functional
- .env.example documents all required environment variables
- Migration files are discoverable by sqlx migrate
- Database connection can be configured via PG_URL
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-schema/01-02-SUMMARY.md`
</output>
